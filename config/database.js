// import knex from 'knex';

// const { DB_HOST, DB_PORT, DB_USER, DB_NAME, DB_CLIENT, NODE_ENV } = process.env;
// console.log(DB_HOST, DB_CLIENT, NODE_ENV);

// const conn = knex({
//    client: DB_CLIENT,
//    connection: {
//       host: DB_HOST,
//       port: DB_PORT,
//       user: DB_USER,
//       database: DB_NAME,
//    },
// });

// conn
//    .raw('Select 1')
//    .then(() => {
//       console.log('Database connected');
//    })
//    .catch((err) => {
//       console.log(err);
//    });

// export default conn;
